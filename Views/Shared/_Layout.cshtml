@inject ICurrentUserService CurrentUser
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - LoanApp</title>
    <script type="importmap"></script>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+Arabic:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/LoanApp.styles.css" asp-append-version="true" />
</head>
<body>
    <header>
    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm mb-4 sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary fs-3" asp-area="" asp-controller="Home" asp-action="Index">
                <i class="bi bi-bank me-2"></i>LoanApp
            </a>
            <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item px-2">
                        <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Home" ? "active fw-bold text-primary" : "text-secondary")" asp-area="" asp-controller="Home" asp-action="Index">الرئيسية</a>
                    </li>
                    @if (CurrentUser.IsAuthenticated)
                    {
                        if (CurrentUser.IsAdmin)
                        {
                            <li class="nav-item px-2">
                                <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Employees" ? "active fw-bold text-primary" : "text-secondary")" asp-area="" asp-controller="Employees" asp-action="Index">الموظفين</a>
                            </li>
                            <li class="nav-item px-2">
                                <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Transactions" ? "active fw-bold text-primary" : "text-secondary")" asp-area="" asp-controller="Transactions" asp-action="Index">التحويلات</a>
                            </li>
                            <li class="nav-item px-2">
                                <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Balances" ? "active fw-bold text-primary" : "text-secondary")" asp-area="" asp-controller="Balances" asp-action="Index">الأرصدة</a>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item px-2">
                                <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Employees" && ViewContext.RouteData.Values["action"]?.ToString() == "MyProfile" ? "active fw-bold text-primary" : "text-secondary")" asp-area="" asp-controller="Employees" asp-action="MyProfile">ملفي الشخصي</a>
                            </li>
                            <li class="nav-item px-2">
                                <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Transactions" ? "active fw-bold text-primary" : "text-secondary")" asp-area="" asp-controller="Transactions" asp-action="Index">تحويلاتي</a>
                            </li>
                            <li class="nav-item px-2">
                                <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Balances" ? "active fw-bold text-primary" : "text-secondary")" asp-area="" asp-controller="Balances" asp-action="Index">رصيدي</a>
                            </li>
                        }
                    }
                </ul>
                <div class="d-flex align-items-center gap-2">
                    @if (CurrentUser.IsAuthenticated)
                    {
                        <span class="badge rounded-pill bg-light text-dark border px-3 py-2">
                            @CurrentUser.EmployeeName <span class="text-muted">(@CurrentUser.Role)</span>
                        </span>
                        <a class="btn btn-outline-secondary btn-sm rounded-pill px-3" asp-controller="Home" asp-action="Login">تبديل المستخدم</a>
                    }
                    else
                    {
                        <a class="btn btn-primary btn-sm rounded-pill px-3" asp-controller="Home" asp-action="Login">تسجيل الدخول</a>
                    }
                </div>
            </div>
        </div>
    </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <!-- Generic Modal for Create Actions -->
    <div class="modal fade" id="actionModal" tabindex="-1" aria-labelledby="actionModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content shadow-lg border-0 rounded-4">
                <div class="modal-header border-0 pb-0">
                    <h5 class="modal-title fw-bold fs-4 text-primary" id="actionModalLabel">Action</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body pt-2" id="modalBodyContent">
                    <!-- Content will be loaded dynamically via Fetch -->
                    <div class="text-center p-5">
                        <div class="spinner-border text-primary" role="status">
                            <span class="visually-hidden">Loading...</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer class="border-top footer text-muted bg-white">
        <div class="container py-3">
            <div class="row align-items-center">
                <div class="col-md-6 text-center text-md-end">
                    &copy; 2026 - <span class="fw-bold text-primary">نظام القروض</span>
                </div>
                <div class="col-md-6 text-center text-md-start">
                    <a asp-area="" asp-controller="Home" asp-action="Privacy" class="text-decoration-none text-muted">سياسة الخصوصية</a>
                </div>
            </div>
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
